import styled from "styled-components";

const ChatHead = () => {
    return (
        <ChatHeadWrapper>
            Chathead
        </ChatHeadWrapper>
    );
}

export default ChatHead;

const ChatHeadWrapper = styled.div`

`;