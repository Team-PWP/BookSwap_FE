import styled from "styled-components";

const ChatMain = () => {
    return (
        <ChatMainWrapper>
            chatmain
        </ChatMainWrapper>
    );
}

export default ChatMain;

const ChatMainWrapper = styled.div`

`;